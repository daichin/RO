-- wifi_secret.lua.example
--
-- 把這個檔複製成 wifi_secret.lua 並填入你的設定，然後跟其他 .lua 一起
-- 上傳到裝置。
--
-- wifi_secret.lua 已經在 .gitignore 裡，不會進版控 —— 密碼不會被 commit
-- 上去，pull 的時候也不會跟你的本機設定打架。
--
-- 不建立這個檔也可以：wifi_cfg.lua 找不到它就走離線模式，
-- 沖洗邏輯完全不受影響，只是沒有網頁介面。

return {
  ssid = "你的 WiFi 名稱",
  pass = "你的密碼",
}
